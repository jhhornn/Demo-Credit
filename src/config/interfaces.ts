export interface DatabaseConfig {
    DB_CLIENT: string;
    DB_HOST: string;
    DB_PORT: number;
    DB_USER: string;
    DB_PASSWORD: string;
    DB_NAME: string;
  }
  
  export interface ServerConfig {
    PORT: number;
  }
  